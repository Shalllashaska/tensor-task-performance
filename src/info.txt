В файле index.html атрибут async для файла stats.js

В contact.js вынес переменные items, itemOffset. 
В методе addContacts задается массив items, так же задается offsetTop 
первого элемента.
В функции вызываемой при скролле считается индекс текущего элемента, и через таймаут изменяется заголовок.